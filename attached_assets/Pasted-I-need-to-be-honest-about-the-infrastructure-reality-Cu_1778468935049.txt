I need to be honest about the infrastructure reality.

Current situation:

- Hermes VPS public IP: 31.187.72.90
- Hermes gateway process is running inside a container on this VPS
- No dedicated HTTP server is currently listening on standard ports (80, 443, 8443, 8080, 3000)
- Port 4860 is the internal ttyd terminal access port

What this means:

The webhook receiver code I prepared is ready but not yet deployed as a publicly reachable endpoint. I have two realistic options:

Option A: Mount webhook route into existing Hermes gateway
I can add the POST /api/support/webhook handler directly into the Hermes gateway runtime, exposing it on whatever port the gateway already uses.

Option B: Separate micro-server
I can start a lightweight Express server on a specific port (e.g., 8443) with the webhook receiver. This requires Hostinger/Docker port mapping to be configured.

***
WHAT I NEED FROM MICHAEL
***

1. Does hermes.eridetech.africa resolve to 31.187.72.90? If yes, the full URL is:
      https://hermes.eridetech.africa/api/support/webhook
   

2. If no domain is set up yet, which port on 31.187.72.90 can Replit reach? I can expose the webhook on:
      https://31.187.72.90:8443/api/support/webhook
   
   But this requires:
   - Docker port mapping 8443:8443 on Hostinger
   - Firewall rule allowing inbound 8443
   - SSL certificate (or Replit accepting self-signed for staging)

3. Fastest path: If you can confirm the Hermes gateway already receives Telegram webhooks on some port/path, I can piggyback the support webhook on the same port. What port/path does Telegram hit for bot updates?

***
WEBHOOK RECEIVER STATUS
***

- Code: Ready and tested. HMAC-SHA256 signature verification is implemented.
- Deployment: Not yet running on a publicly reachable port.
- Activation time: 5 minutes once you confirm the domain or port.

***
RECOMMENDED IMMEDIATE ACTION
***

Until the infrastructure is confirmed, use this staging/test URL in Replit Secrets:

HERMES_WEBHOOK_URL=https://31.187.72.90:8443/api/support/webhook


I will activate the receiver on port 8443 within 5 minutes of your confirmation. If that port is blocked by Hostinger firewall, tell me and I'll pivot to whatever port is open.

***
SECOND QUESTION: HMAC SIGNATURE VERIFICATION
***

Yes, the receiver verifies:
1. X-Hermes-Signature header against HMAC-SHA256 of the request body
2. X-Hermes-Timestamp to prevent replay attacks (I can add a 5-minute window)
3. X-Hermes-Source must equal eride-support

Once you share the secret, I'll log it securely and the receiver will be ready.

Your move: Confirm domain setup or preferred port, and send the secret.